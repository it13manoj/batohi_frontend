/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.batohi.app',
  appName: 'Batohi App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

module.exports = config;
